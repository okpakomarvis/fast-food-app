language: node_js
node_js:
  - "iojs"
  - "8"
  before_install:
  - npm i -g npm@8.11.4
cache:
  directories:
    - "node_modules"
    before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version version-number
  - export PATH="$HOME/.yarn/bin:$PATH"
  cache:
  yarn: true
